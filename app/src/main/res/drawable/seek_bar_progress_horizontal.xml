<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:angle="270"
                android:centerColor="@color/line_gray"
                android:centerY="0.75"
                android:endColor="@color/line_gray"
                android:startColor="@color/line_gray" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/line_gray"
                    android:centerY="0.75"
                    android:endColor="@color/line_gray"
                    android:startColor="@color/line_gray" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/green_dark"
                    android:centerY="0.75"
                    android:endColor="@color/green_dark"
                    android:startColor="@color/green_dark" />
            </shape>
        </clip>
    </item>
</layer-list>

